# TEMPLATE - Copy to forgejo-registry-secret.sops.yaml and encrypt with SOPS
# sops --encrypt --age $(cat ~/.sops/age-recipients.txt) forgejo-registry-secret.template.yaml > forgejo-registry-secret.sops.yaml

apiVersion: v1
kind: Secret
metadata:
  name: forgejo-registry-creds
  namespace: argocd
type: Opaque
stringData:
  username: "REPLACE_WITH_FORGEJO_USERNAME"
  password: "REPLACE_WITH_FORGEJO_ACCESS_TOKEN"
