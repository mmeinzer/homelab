# TEMPLATE - Copy to forgejo-secrets.sops.yaml and encrypt with SOPS
# sops --encrypt --age $(cat ~/.sops/age-recipients.txt) forgejo-secrets.template.yaml > forgejo-secrets.sops.yaml

apiVersion: v1
kind: Secret
metadata:
  name: forgejo-admin
  namespace: forgejo
type: Opaque
stringData:
  password: "REPLACE_WITH_SECURE_PASSWORD"
  username: "admin"
